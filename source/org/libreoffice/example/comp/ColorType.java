package org.libreoffice.example.comp;

/**
 * This enum allows us to have one method in AnalogClockPanel to get and method to set any color instead of one get and one set per color.
 */
public enum ColorType {
	FaceColor, HourHandColor, MinuteHandColor, SecondHandColor
}
